// Zachary Jorgensen
// 10/4/16

#include<iostream>
using namespace std;

int main(){
    cout << "1) Since suzyAge is not less than johnAge, it is false, 0." << endl;
    cout << "2) Since x and y are equal, the statement is true, 1." << endl;
    cout << "3) Because a and b are not equal to each other, the statement is false, 0." << endl;
    cout << "4) Since (20 * 10) / 2 = 100 and because 100 is greater than 0, it is a true statement, 1." << endl;
    cout << "5) Although -4 is not greater than 5, 0 is less than 2 making the statement true, 1." << endl;
    cout << "6) Although -5 is not greater than 0, the ! reverses the statement from false to true, 1." << endl;
    cout << "7) Both sides must be true in order for it to output true, they indeed are both true, so it is 1." << endl;
    cout << "8) z was not declared causing an error." << endl;
    cout << "9) y and x were not declared causing an error." << endl;
    
    return 0;
}